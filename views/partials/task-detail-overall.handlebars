<div class="columns transparentItem ml-5 mr-5 mt-0 mb-0" style="border-bottom: solid; border-color:  white; border-width: thin;">
  <div class="column has-text-centered is-1">
  {{math @index "+" 1}}
  </div>
  <div class="column has-text-centered is-2">
  {{name}}
  </div>
  <div class="column has-text-centered is-2">
  <div class="select has-text-centered is-rounded is-small">
    <select data-id="{{id}}" class="statusBtn">
      <option value="{{status}}">{{status}}</option>
      <option value="{{option1 status}}">{{option1 status}}</option>
      <option value="{{option2 status}}">{{option2 status}}</option>
      <option value="{{option3 status}}">{{option3 status}}</option>
    </select>
  </div>
  </div>
  <div class="column has-text-centered is-2">
  {{project.name}}
  </div>
  <div class="column has-text-centered is-2">
  {{due_date}}
  </div>
  <div class="column is-3">
  {{comments.[0].description}}
  </div>
</div>